# Проект по использованию python, selenium, pytest

## Этапы

1.
```
Открыть папку с проектом в gitbash
```
2. 
```
Проверить версию пайтон (py --version)
```
3.
```
Создать папку venv (python"version python" -m venv "name")
```
4. 
```
Скачать .gitignore(github)
```
5.
```
Добавить в .gitignore "name"(чтобы не трогать папку)
```
6.
``` 
Активировать вертуальное окружение(source "name"/Scripts/activate)
```
7.
```
Установка selenium(pip install selenium)
```
8. 
```
Установка requirements.txt(pip freezer > requirements.txt)
```
3. list of commits
```
git log -- oneline -N
```
4. Создание ssh соединения
```
ssh-keygen -t ed25519 -C "@".

ls -la.

ssh -T git@name.ru(com)

clip < id_ed.

Вставить в github.

```
5. Config Настройка
```
Host name.ru(com)
	HostName github.com
	User name
	IdentityFile ~/.ssh/namefile
	IdentitiesOnly=yes
```
6. Проверить ветки
```
git branch -v
```
7. Создать ветку 
```
git switch -c "name"
```
8. Перейти на ветку 
```
git switch "name"
```
